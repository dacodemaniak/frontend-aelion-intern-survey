import { BubbleDirective } from './bubble.directive';

describe('BubbleDirective', () => {
  it('should create an instance', () => {
    const directive = new BubbleDirective();
    expect(directive).toBeTruthy();
  });
});
